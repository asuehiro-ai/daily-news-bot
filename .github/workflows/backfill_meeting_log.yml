name: 面談ログ・過去分バックフィル

on:
  workflow_dispatch:
    inputs:
      from_date:
        description: '開始日 (YYYY-MM-DD)'
        required: true
      to_date:
        description: '終了日 (YYYY-MM-DD、省略時は前日)'
        required: false
      dry_run:
        description: 'true にすると書き込みせず内容確認のみ'
        required: false
        default: 'true'

jobs:
  backfill:
    runs-on: ubuntu-latest

    steps:
      - name: リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: Python 3.11 のセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 依存パッケージのインストール
        run: pip install -r requirements.txt

      - name: 過去分バックフィル実行
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PLAUD_TOKEN: ${{ secrets.PLAUD_TOKEN }}
          PLAUD_BASE_URL: ${{ secrets.PLAUD_BASE_URL }}
          FROM_DATE: ${{ github.event.inputs.from_date }}
          TO_DATE: ${{ github.event.inputs.to_date }}
          DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          ARGS="--from $FROM_DATE"
          if [ -n "$TO_DATE" ]; then ARGS="$ARGS --to $TO_DATE"; fi
          if [ "$DRY_RUN" = "true" ]; then ARGS="$ARGS --dry-run"; fi
          python meeting-log-sync/backfill_meeting_log.py $ARGS
